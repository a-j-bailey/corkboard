/** User-visible status lines during poster → preview processing. */
export const ExtractionStepMessage = {
  extracting: 'Extracting information…',
  identifyingLocation: 'Identifying location…',
  lookingUpLocation: 'Looking up the location…',
} as const;
